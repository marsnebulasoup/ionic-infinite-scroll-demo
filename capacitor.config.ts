import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-infinite-scroll-demo',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
